<Window x:Class="SoundPooper.MVVM.Views.SoundPooperView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:rm="clr-namespace:RadialMenu.Controls;assembly=RadialMenu"
        mc:Ignorable="d"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ShowInTaskbar="False"
        Top="{Binding WindowTopLeftY, Mode=OneWay}"
        Left="{Binding WindowTopLeftX, Mode=OneWay}"
        Cursor="Hand"
        DataContext="{Binding SoundPooperViewModel,
        Source={StaticResource Locator}}"
        Title="SoundPooperView"
        Height="600"
        Width="600">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="MouseMove">
            <i:InvokeCommandAction Command="{Binding MouseMoveCommand}"
                                   PassEventArgsToCommand="True" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <rm:RadialMenu
            Content="{Binding SoundList, Mode=OneTime}"
            IsOpen="True"
            Background="Red"
            Height="600"
            Width="600">
            <rm:RadialMenu.CentralItem>
                <rm:RadialMenuCentralItem Background="Red">
                    <TextBlock>Close</TextBlock>
                </rm:RadialMenuCentralItem>
            </rm:RadialMenu.CentralItem>
        </rm:RadialMenu>
    </Grid>
</Window>