<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ex="clr-namespace:SoundPooper.MVVM.Extensions">
    <Style TargetType="{x:Type Button}" x:Key="RoundedButtonStyle">
        <Setter Property="FontSize" Value="16" />
        <Setter
            Property="ex:ButtonExtensions.LabelBackground"
            Value="Bisque" />
        <Setter Property="ex:ButtonExtensions.CornerRadius" Value="50" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border
                        Background="{TemplateBinding Background}"
                        Height="{TemplateBinding Height}"
                        Width="{TemplateBinding Width}"
                        CornerRadius="{Binding 
                            Path=(ex:ButtonExtensions.CornerRadius), 
                            RelativeSource={RelativeSource TemplatedParent}
                        }">
                        <Border
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Padding="5, 3"
                            CornerRadius="5"
                            Background="{Binding
                            Path=(ex:ButtonExtensions.LabelBackground),
                            RelativeSource={RelativeSource TemplatedParent}}">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>